{% extends "_layouts/site" %}

{% block content  %}
    <div class="container-fluid">
        <div class="card card-rounded bg-primary mt-4 mt-md-0">
            <div class="card-body">
                <div class="col-sm-12 text-center">
                    <h2 class="text-white upgrade-info mb-0">Page Not Found</h2>
                </div>
            </div>
        </div>

        <!-- 404 Error Text -->
        <div class="text-center ">
            <h2 class="heading m-5">404</h2>
            <p class="text-gray-500 m">It looks like you found a glitch in the matrix...</p>
            <a href="{{ siteUrl }}">← Back to Dashboard</a>
        </div>
    </div>
{% endblock %}
