{% extends "_layouts/site" %}
{#
    Safety category detail page
#}
{% block content  %}
    <div class="card card-rounded bg-primary mt-4 mt-md-0">
        <div class="card-body">
            <div class="col-sm-12 text-center">
                <h2 class="text-white upgrade-info mb-0">{{ entry.title }}</h2>
            </div>
        </div>
    </div>
        <div class="row">
            <div class="col-lg-12 d-flex flex-column">
                <div class="row">
                    {% set thumbnail = entry.safetySigns.one() %}
                    <div class="col-lg-12 mt-3">
                        <section class=" h-100 card card-rounded justify-content-around text-dark ">
                            <div class=" grid-margin d-flex flex-column  stretch-card card-rounded">
                                <div class="card-body">
                                    <p>
                                        {{ entry.safetyDescription }}
                                    </p>
                                </div>
                            </div>
                        </section>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
